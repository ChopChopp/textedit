/**
 * Command handles the possible commands which can
 * be used by the user to interact with the document
 */

public enum Command {
    ADD,
    DEL,
    DUMMY,
    EXIT,
    FORMAT_FIX,
    FORMAT_RAW,
    INDEX,
    PRINT,
    REPLACE,
    HELP;
}
